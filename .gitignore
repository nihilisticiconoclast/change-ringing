__pycache__/
*.pyc
*.db
# ...except the committed snapshot. Working copies stay ignored; this one file
# is deliberately tracked -- read data/LICENCE-DATA.md before updating it, and
# note that every re-commit adds another ~40 MB to history permanently.
!data/change-ringing.db
dove-csvs/
methods-cache/
*.xml
*.zip

# offline replica and its build cache
local_corpus.db
*.db-shm
*.db-wal
.local-build-cache/
