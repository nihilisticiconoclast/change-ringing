__pycache__/
*.pyc
*.db
# NOTE: data/change-ringing.db was previously tracked but exceeded GitHub's
# 100 MB file size limit. It is now ignored. Use Git LFS if you need to
# track large database files, or keep it as a local-only file.
dove-csvs/
methods-cache/
complib-cache/
*.xml
*.zip

# offline replica and its build cache
local_corpus.db
*.db-shm
*.db-wal
.local-build-cache/
data/change-ringing.db
*checkpoint*.json

# from PR #13: the API-walk scratch samples are not tracked, but the
# committed corpus CSVs are (data/complib/compositions.csv,
# composition_methods.csv). Ignore everything else under data/complib/.
data/complib/*
!data/complib/compositions.csv
!data/complib/composition_methods.csv
scratch/
*_output.txt
*.log
